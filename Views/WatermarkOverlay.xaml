<Window x:Class="TwoTo1Screen.Views.WatermarkOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" ShowActivated="False" Focusable="False"
        IsHitTestVisible="False" Topmost="True"
        SizeToContent="WidthAndHeight"
        FontFamily="Segoe UI" TextOptions.TextFormattingMode="Ideal">
    <Grid Margin="0">
        <StackPanel HorizontalAlignment="Right">
            <TextBlock x:Name="Line1" Text="2to1screen — не активировано"
                       FontSize="20" FontWeight="SemiBold" Foreground="#B6FFFFFF"
                       HorizontalAlignment="Right" TextAlignment="Right">
                <TextBlock.Effect>
                    <DropShadowEffect Color="#000000" BlurRadius="6" ShadowDepth="0" Opacity="0.7" />
                </TextBlock.Effect>
            </TextBlock>
            <TextBlock x:Name="Line2" Text="Активируйте программу 2to1screen"
                       FontSize="14" Foreground="#96FFFFFF"
                       HorizontalAlignment="Right" TextAlignment="Right" Margin="0,2,0,0">
                <TextBlock.Effect>
                    <DropShadowEffect Color="#000000" BlurRadius="6" ShadowDepth="0" Opacity="0.7" />
                </TextBlock.Effect>
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>
